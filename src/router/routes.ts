export enum ROUTE {
  HOME = '/',
  FAVORITES = 'favorites',
  SEARCH = 'search',
  SIGN_IN = 'sign-in',
  SIGN_UP = 'sign-up',
  TRENDS = 'trends',
  MOVIE = 'movie/:imdbID/:title',
  RESET_PASSWORD = 'reset-password',
  SETTINGS = 'settings',
}
