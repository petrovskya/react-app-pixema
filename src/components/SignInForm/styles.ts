import styled from "styled-components";
import { Color } from "ui";

export const StyledSignInForm = styled.form`
  display: grid;
  gap: 20px;
  width: 100%;
  font-size: 16px;
  font-weight: 600;
  line-height: 24px;
  text-align: left;
  color: ${Color.WHITE};
`;
